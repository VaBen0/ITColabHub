<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <EditText
        android:id="@+id/name_link"
        android:layout_width="0dp"
        android:layout_height="45dp"
        android:textSize="15sp"
        android:layout_marginTop="5dp"
        android:layout_marginEnd="10dp"
        android:backgroundTint="@color/transparent"
        android:hint="Введите название ссылки"
        app:layout_constraintEnd_toStartOf="@+id/deleteBut"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/deleteBut"
        android:layout_width="30dp"
        android:layout_height="30dp"
        app:layout_constraintBottom_toBottomOf="@+id/name_link"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="?trash_icon" />

    <EditText
        android:id="@+id/link_main"
        android:layout_width="0dp"
        android:layout_height="45dp"
        android:textSize="15sp"
        android:layout_marginTop="-10dp"
        android:backgroundTint="@color/transparent"
        android:hint="Вставьте ссылку"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/name_link" />
</androidx.constraintlayout.widget.ConstraintLayout>